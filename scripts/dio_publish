#!/usr/bin/env sh 
git add -A
git commit -m "[publish]"
git push origin master

hcloud context use divramod.io
ip=$(hcloud server list | tail -n +2 | fzf | awk '{ print $4; }')
ssh -o "StrictHostKeyChecking no" root@"${ip}" '/var/www/divramod.io/dio_render'
